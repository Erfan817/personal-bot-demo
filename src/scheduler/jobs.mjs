/**
 * 调度层 · 内置默认任务
 * ═══════════════════════════════════════════════════
 * ⚠️ 这个文件现在只提供【首次运行的默认值】。
 *
 * 真正的任务清单在 data/jobs.json —— 由 index.mjs 在首次启动时
 * 从这里生成，之后就以那个 JSON 为准（支持热重载）。
 *
 * 为什么改成这样：
 *   任务定义是【运行时配置】，不是代码。
 *   想调个提醒时间不该走一遍"改代码 → 推送 → 重启"的流程。
 *
 * 字段：
 *   name    任务名（会出现在推送里）
 *   cron    "分 时 日 月 周"（服务器本地时间 = 北京时间）
 *   prompt  到点时喂给 agent 的指令
 *   chatId  推送到哪个会话（不填就用 .env 里的 SCHEDULE_CHAT_ID）
 *   enabled 设 false 可关掉
 */
export const defaultJobs = [
  {
    name: "每日早报",
    cron: "0 8 * * *", // 每天 08:00
    prompt:
      "生成今天的早报，包含两部分：\n" +
      "1. 今天是几月几日、星期几\n" +
      "2. 一句简短的、积极的话\n" +
      "用中文，控制在 5 行以内。",
    enabled: false,
  },

  // 更多例子（复制到 data/jobs.json 里改就行）
  //
  // {
  //   name: "周五收尾提醒",
  //   cron: "0 17 * * 5",
  //   prompt: "提醒用户整理本周待办，列出还没完成的事项。",
  //   enabled: true
  // },
  //
  // {
  //   name: "每天两次喝水提醒",
  //   cron: "0 10,15 * * *",
  //   prompt: "用一句话提醒用户喝水，语气轻松。",
  //   enabled: true
  // }
];